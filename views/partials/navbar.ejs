<nav class="navbar navbar-expand-lg ek-navbar">
    <div class="container">
        <a class="navbar-brand" href="/">
            <span class="brand-icon"><i class="bi bi-stars"></i></span>
            EventKraft
        </a>
        <% if (locals.isOnboarding) { %>
            <ul class="navbar-nav ms-auto flex-row gap-3 align-items-center">
                <li class="nav-item"><a class="nav-link" href="/gigs">Browse Services</a></li>
                <li class="nav-item"><a class="nav-link" href="/jobs">Browse Gigs</a></li>
            </ul>
        <% } else { %>
        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav"
            aria-label="Toggle navigation">
            <i class="bi bi-list fs-4" style="color:var(--ek-text)"></i>
        </button>
        <div class="collapse navbar-collapse" id="mainNav">
            <ul class="navbar-nav me-auto">
                <% if (currentUser) { %>
                    <% if (currentUser.role==='customer' ) { %>
                        <li class="nav-item"><a class="nav-link" href="/gigs">Browse Services</a></li>
                        <li class="nav-item"><a class="nav-link" href="/jobs/create">Post a Gig</a></li>
                        <% } else if (currentUser.role==='worker' ) { %>
                            <li class="nav-item"><a class="nav-link" href="/jobs">Browse Gigs</a></li>
                            <li class="nav-item"><a class="nav-link" href="/gigs/create">Create Service</a></li>
                            <% } else if (currentUser.role==='admin' ) { %>
                                <li class="nav-item"><a class="nav-link" href="/gigs">Browse Services</a></li>
                                <li class="nav-item"><a class="nav-link" href="/jobs">Browse Gigs</a></li>
                                <% } %>
                                    <% } else { %>
                                        <li class="nav-item"><a class="nav-link" href="/gigs">Browse Services</a></li>
                                        <li class="nav-item"><a class="nav-link" href="/jobs">Browse Gigs</a></li>
                                        <li class="nav-item"><a class="nav-link" href="/auth/register">Post a Gig</a></li>
                                        <% } %>
            </ul>
            <ul class="navbar-nav ms-auto align-items-lg-center">
                <% if (currentUser) { %>
                    <li class="nav-item">
                        <div class="topnav__icon-wrapper" id="notif-wrapper">
                            <a href="/dashboard/notifications" class="nav-link ek-nav-icon" id="notif-btn" title="Notifications">
                                <i class="bi bi-bell-fill fs-5"></i>
                                <span class="ek-nav-badge <%= (locals.unreadNotifs || 0) === 0 ? 'd-none' : '' %>" id="notif-badge"><%= locals.unreadNotifs || 0 %></span>
                            </a>
                            <div class="topnav__dropdown topnav__dropdown--notif" id="notif-dropdown">
                                <div class="topnav__dropdown-head">
                                    <strong>Notifications</strong>
                                    <button class="btn-clear-all" id="clear-all-notifs">Clear All</button>
                                </div>
                                <div class="topnav__dropdown-list" id="notif-list">
                                    <div class="topnav__dropdown-empty">Loading notifications...</div>
                                </div>
                                <a href="/dashboard/notifications" class="topnav__dropdown-foot">View All Notifications</a>
                            </div>
                        </div>
                    </li>
                    <li class="nav-item">
                        <div class="topnav__icon-wrapper" id="msg-wrapper">
                            <a class="nav-link ek-nav-icon" href="/messages" title="Messages" id="navMsgLink">
                                <i class="bi bi-chat-dots-fill fs-5"></i>
                                <span class="ek-nav-badge <%= (locals.unreadMsgs || 0) === 0 ? 'd-none' : '' %>" id="msg-badge"><%= locals.unreadMsgs || 0 %></span>
                            </a>
                            <div class="topnav__dropdown topnav__dropdown--msg" id="msg-dropdown">
                                <div class="topnav__dropdown-head">
                                    <strong>Recent Messages</strong>
                                </div>
                                <div class="topnav__dropdown-list" id="msg-pop-list">
                                    <div class="topnav__dropdown-empty">Loading messages...</div>
                                </div>
                                <a href="/messages" class="topnav__dropdown-foot">Go to Messenger</a>
                            </div>
                        </div>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/dashboard"><i class="bi bi-speedometer2"></i> Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <button type="button" class="ek-theme-toggle" data-theme-toggle aria-label="Switch color theme">
                            <i class="bi bi-moon-stars" data-theme-icon></i>
                            <span data-theme-label>Dark</span>
                        </button>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">
                            <i class="bi bi-person-circle"></i>
                            <%= currentUser.email.split('@')[0] %>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="/dashboard"><i
                                        class="bi bi-speedometer2 me-2"></i>Dashboard</a></li>
                            <% if (currentUser.role==='admin' ) { %>
                                <li><a class="dropdown-item" href="/admin"><i class="bi bi-shield-check me-2"></i>Admin
                                        Panel</a></li>
                                <% } %>
                            <li><a class="dropdown-item" href="/dashboard/settings"><i class="bi bi-gear me-2"></i>Settings</a></li>
                                    <li>
                                        <hr class="dropdown-divider">
                                    </li>
                                    <li><a class="dropdown-item text-danger" href="/auth/logout"><i
                                                class="bi bi-box-arrow-right me-2"></i>Log out</a></li>
                        </ul>
                    </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="btn btn-ek-ghost" href="/auth/login">Log in</a>
                        </li>
                        <li class="nav-item ms-lg-2">
                            <button type="button" class="ek-theme-toggle" data-theme-toggle aria-label="Switch color theme">
                                <i class="bi bi-moon-stars" data-theme-icon></i>
                                <span data-theme-label>Dark</span>
                            </button>
                        </li>
                        <li class="nav-item ms-lg-2">
                            <a class="btn btn-ek-primary" href="/auth/register">Get started</a>
                        </li>
                        <% } %>
            </ul>
        </div>
        <% } %>
    </div>
</nav>
