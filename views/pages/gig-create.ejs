<%- include('../partials/header') %>

    <section class="container py-5" style="max-width:780px">
        <div class="text-center mb-4">
            <h1 class="font-display fs-2 fw-semibold">Create a Service</h1>
            <p class="text-muted">Showcase your skills and start getting bookings.</p>
        </div>

        <form action="/gigs" method="POST" enctype="multipart/form-data" id="gigForm">

            <!-- ═══════════════════════════════════════════════════════ -->
            <!-- SECTION 1: Service Details                             -->
            <!-- ═══════════════════════════════════════════════════════ -->
            <div class="card shadow-sm mb-4">
                <div class="card-header bg-white">
                    <h5 class="mb-0"><i class="bi bi-1-circle-fill text-primary me-2"></i>Service Details</h5>
                </div>
                <div class="card-body">
                    <div class="mb-3">
                        <label class="form-label" for="title">Service Title *</label>
                        <input type="text" class="form-control" id="title" name="title" required
                            placeholder="e.g. Professional Wedding Photography">
                    </div>

                    <div class="row g-3 mb-3">
                        <div class="col-md-6">
                            <label class="form-label" for="category_id">Category *</label>
                            <select class="form-select" id="category_id" name="category_id" required>
                                <option value="" disabled selected>Select a category</option>
                                <% categories.forEach(function(c) { %>
                                    <option value="<%= c.id %>">
                                        <%= c.name %>
                                    </option>
                                    <% }); %>
                            </select>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label" for="starting_price">Starting Price (NPR) *</label>
                            <input type="number" class="form-control" id="starting_price" name="starting_price" required
                                min="100" step="100" placeholder="e.g. 25000">
                            <div class="form-text">This is the base price shown in search results.</div>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label" for="description">Description *</label>
                        <textarea class="form-control" id="description" name="description" rows="5" required
                            placeholder="Describe your service, experience, what's included…"></textarea>
                    </div>

                    <div class="mb-3">
                        <label class="form-label" for="delivery_time">Delivery Time</label>
                        <input type="text" class="form-control" id="delivery_time" name="delivery_time"
                            placeholder="e.g. 3-7 days">
                    </div>
                </div>
            </div>

            <!-- ═══════════════════════════════════════════════════════ -->
            <!-- SECTION 2: Portfolio Images                            -->
            <!-- ═══════════════════════════════════════════════════════ -->
            <div class="card shadow-sm mb-4">
                <div class="card-header bg-white">
                    <h5 class="mb-0"><i class="bi bi-2-circle-fill text-primary me-2"></i>Portfolio Images</h5>
                </div>
                <div class="card-body">
                    <p class="text-muted small mb-3">Upload up to 5 images of your previous work. (JPG, PNG, WebP — max
                        5MB each)</p>

                    <div id="dropZone" class="border border-2 border-dashed rounded-3 p-4 text-center"
                        style="cursor:pointer; transition: all 0.2s; border-color: #dee2e6;">
                        <i class="bi bi-cloud-arrow-up fs-1 text-muted"></i>
                        <p class="text-muted mb-1">Drag & drop images here or click to browse</p>
                        <p class="text-muted small mb-0">Up to 5 images (JPG, PNG, WebP)</p>
                        <input type="file" id="imageInput" name="portfolio_images" multiple
                            accept="image/jpeg,image/png,image/webp,image/gif" class="d-none">
                    </div>

                    <div id="imagePreview" class="row g-2 mt-2"></div>
                </div>
            </div>

            <!-- ═══════════════════════════════════════════════════════ -->
            <!-- SECTION 3: Pricing Tiers (Optional)                    -->
            <!-- ═══════════════════════════════════════════════════════ -->
            <div class="card shadow-sm mb-4">
                <div class="card-header bg-white d-flex justify-content-between align-items-center">
                    <h5 class="mb-0"><i class="bi bi-3-circle-fill text-primary me-2"></i>Pricing Tiers <span
                            class="badge bg-secondary ms-1">Optional</span></h5>
                    <div class="form-check form-switch mb-0">
                        <input class="form-check-input" type="checkbox" id="enableTiers" style="cursor:pointer">
                        <label class="form-check-label small" for="enableTiers" style="cursor:pointer">Add tiers</label>
                    </div>
                </div>
                <div class="card-body" id="tiersSection" style="display:none">
                    <p class="text-muted small mb-3">Define pricing tiers to give customers options.</p>

                    <div class="accordion" id="tierAccordion">

                        <!-- BASIC Tier -->
                        <div class="accordion-item tier-card" data-tier="basic">
                            <h2 class="accordion-header">
                                <button class="accordion-button" type="button" data-bs-toggle="collapse"
                                    data-bs-target="#tierBasic">
                                    <i class="bi bi-star me-2"></i> Basic
                                </button>
                            </h2>
                            <div id="tierBasic" class="accordion-collapse collapse show"
                                data-bs-parent="#tierAccordion">
                                <div class="accordion-body">
                                    <input type="hidden" name="pkg_tier" value="basic" class="tier-hidden" disabled>
                                    <div class="row g-3">
                                        <div class="col-md-6">
                                            <label class="form-label small">Package Title *</label>
                                            <input type="text" class="form-control form-control-sm tier-input"
                                                name="pkg_title" placeholder="e.g. Starter Package" disabled>
                                        </div>
                                        <div class="col-md-3">
                                            <label class="form-label small">Price (NPR) *</label>
                                            <input type="number" class="form-control form-control-sm tier-input"
                                                name="pkg_price" min="100" step="100" placeholder="5000" disabled>
                                        </div>
                                        <div class="col-md-3">
                                            <label class="form-label small">Delivery</label>
                                            <input type="text" class="form-control form-control-sm tier-input"
                                                name="pkg_delivery_time" placeholder="3 days" disabled>
                                        </div>
                                        <div class="col-12">
                                            <label class="form-label small">Description</label>
                                            <textarea class="form-control form-control-sm tier-input"
                                                name="pkg_description" rows="2"
                                                placeholder="What's included in this tier…" disabled></textarea>
                                        </div>
                                        <div class="col-12">
                                            <label class="form-label small">Features <span
                                                    class="text-muted">(comma-separated)</span></label>
                                            <input type="text" class="form-control form-control-sm tier-input"
                                                name="pkg_features"
                                                placeholder="e.g. 50 photos, Basic editing, Digital delivery" disabled>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- STANDARD Tier -->
                        <div class="accordion-item tier-card" data-tier="standard">
                            <h2 class="accordion-header">
                                <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse"
                                    data-bs-target="#tierStandard">
                                    <i class="bi bi-star-half me-2"></i> Standard
                                </button>
                            </h2>
                            <div id="tierStandard" class="accordion-collapse collapse" data-bs-parent="#tierAccordion">
                                <div class="accordion-body">
                                    <input type="hidden" name="pkg_tier" value="standard" class="tier-hidden" disabled>
                                    <div class="row g-3">
                                        <div class="col-md-6">
                                            <label class="form-label small">Package Title *</label>
                                            <input type="text" class="form-control form-control-sm tier-input"
                                                name="pkg_title" placeholder="e.g. Professional Package" disabled>
                                        </div>
                                        <div class="col-md-3">
                                            <label class="form-label small">Price (NPR) *</label>
                                            <input type="number" class="form-control form-control-sm tier-input"
                                                name="pkg_price" min="100" step="100" placeholder="15000" disabled>
                                        </div>
                                        <div class="col-md-3">
                                            <label class="form-label small">Delivery</label>
                                            <input type="text" class="form-control form-control-sm tier-input"
                                                name="pkg_delivery_time" placeholder="5 days" disabled>
                                        </div>
                                        <div class="col-12">
                                            <label class="form-label small">Description</label>
                                            <textarea class="form-control form-control-sm tier-input"
                                                name="pkg_description" rows="2"
                                                placeholder="What's included in this tier…" disabled></textarea>
                                        </div>
                                        <div class="col-12">
                                            <label class="form-label small">Features <span
                                                    class="text-muted">(comma-separated)</span></label>
                                            <input type="text" class="form-control form-control-sm tier-input"
                                                name="pkg_features"
                                                placeholder="e.g. 150 photos, Advanced editing, Album" disabled>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- PREMIUM Tier -->
                        <div class="accordion-item tier-card" data-tier="premium">
                            <h2 class="accordion-header">
                                <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse"
                                    data-bs-target="#tierPremium">
                                    <i class="bi bi-star-fill me-2"></i> Premium
                                </button>
                            </h2>
                            <div id="tierPremium" class="accordion-collapse collapse" data-bs-parent="#tierAccordion">
                                <div class="accordion-body">
                                    <input type="hidden" name="pkg_tier" value="premium" class="tier-hidden" disabled>
                                    <div class="row g-3">
                                        <div class="col-md-6">
                                            <label class="form-label small">Package Title *</label>
                                            <input type="text" class="form-control form-control-sm tier-input"
                                                name="pkg_title" placeholder="e.g. VIP Package" disabled>
                                        </div>
                                        <div class="col-md-3">
                                            <label class="form-label small">Price (NPR) *</label>
                                            <input type="number" class="form-control form-control-sm tier-input"
                                                name="pkg_price" min="100" step="100" placeholder="50000" disabled>
                                        </div>
                                        <div class="col-md-3">
                                            <label class="form-label small">Delivery</label>
                                            <input type="text" class="form-control form-control-sm tier-input"
                                                name="pkg_delivery_time" placeholder="7 days" disabled>
                                        </div>
                                        <div class="col-12">
                                            <label class="form-label small">Description</label>
                                            <textarea class="form-control form-control-sm tier-input"
                                                name="pkg_description" rows="2"
                                                placeholder="What's included in this tier…" disabled></textarea>
                                        </div>
                                        <div class="col-12">
                                            <label class="form-label small">Features <span
                                                    class="text-muted">(comma-separated)</span></label>
                                            <input type="text" class="form-control form-control-sm tier-input"
                                                name="pkg_features"
                                                placeholder="e.g. Unlimited photos, Drone shots, Premium album"
                                                disabled>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                    </div>
                </div>
            </div>

            <!-- Action Buttons -->
            <input type="hidden" name="status" value="active">
            <div class="d-flex flex-wrap gap-2">
                <button type="submit" class="btn btn-primary btn-lg">
                    <i class="bi bi-check-lg me-1"></i> Publish Service
                </button>
                <button type="submit" name="status" value="draft" class="btn btn-outline-secondary btn-lg">
                    Save as Draft
                </button>
                <a href="/auth/dashboard" class="btn btn-outline-danger btn-lg ms-auto">Cancel</a>
            </div>
        </form>
    </section>

    <style>
        #dropZone.dragover {
            border-color: var(--bs-primary) !important;
            background: rgba(13, 110, 253, 0.04);
        }

        .img-preview-card {
            position: relative;
            border-radius: 8px;
            overflow: hidden;
        }

        .img-preview-card img {
            width: 100%;
            height: 120px;
            object-fit: cover;
            border-radius: 8px;
        }

        .img-preview-card .remove-btn {
            position: absolute;
            top: 4px;
            right: 4px;
            width: 24px;
            height: 24px;
            padding: 0;
            border-radius: 50%;
            background: rgba(220, 53, 69, 0.9);
            color: #fff;
            border: none;
            font-size: 14px;
            line-height: 24px;
            cursor: pointer;
        }

        .tier-card .accordion-button:not(.collapsed) {
            background-color: rgba(13, 110, 253, 0.05);
        }
    </style>

    <script>
        (function () {
            const dropZone = document.getElementById('dropZone');
            const imageInput = document.getElementById('imageInput');
            const previewGrid = document.getElementById('imagePreview');
            const MAX_FILES = 5;
            let fileStore = new DataTransfer();

            dropZone.addEventListener('click', () => imageInput.click());
            dropZone.addEventListener('dragover', e => { e.preventDefault(); dropZone.classList.add('dragover'); });
            dropZone.addEventListener('dragleave', () => dropZone.classList.remove('dragover'));
            dropZone.addEventListener('drop', e => { e.preventDefault(); dropZone.classList.remove('dragover'); addFiles(e.dataTransfer.files); });
            imageInput.addEventListener('change', () => { addFiles(imageInput.files); });

            function addFiles(newFiles) {
                for (const file of newFiles) {
                    if (fileStore.files.length >= MAX_FILES) break;
                    if (!file.type.startsWith('image/')) continue;
                    fileStore.items.add(file);
                }
                imageInput.files = fileStore.files;
                renderPreviews();
            }

            function removeFile(index) {
                const newStore = new DataTransfer();
                for (let i = 0; i < fileStore.files.length; i++) {
                    if (i !== index) newStore.items.add(fileStore.files[i]);
                }
                fileStore = newStore;
                imageInput.files = fileStore.files;
                renderPreviews();
            }

            function renderPreviews() {
                previewGrid.innerHTML = '';
                for (let i = 0; i < fileStore.files.length; i++) {
                    const file = fileStore.files[i];
                    const col = document.createElement('div'); col.className = 'col-4 col-md-2';
                    const card = document.createElement('div'); card.className = 'img-preview-card';
                    const img = document.createElement('img'); img.src = URL.createObjectURL(file); img.alt = file.name;
                    const btn = document.createElement('button'); btn.type = 'button'; btn.className = 'remove-btn'; btn.innerHTML = '&times;'; btn.onclick = () => removeFile(i);
                    card.appendChild(img); card.appendChild(btn); col.appendChild(card); previewGrid.appendChild(col);
                }
                const remaining = MAX_FILES - fileStore.files.length;
                dropZone.querySelector('p:first-of-type').textContent = fileStore.files.length > 0
                    ? `${fileStore.files.length} image${fileStore.files.length > 1 ? 's' : ''} selected` + (remaining > 0 ? ` — ${remaining} more allowed` : '')
                    : 'Drag & drop images here or click to browse';
            }

            const tiersToggle = document.getElementById('enableTiers');
            const tiersSection = document.getElementById('tiersSection');
            tiersToggle.addEventListener('change', function () {
                tiersSection.style.display = this.checked ? 'block' : 'none';
                document.querySelectorAll('.tier-input').forEach(el => el.disabled = !this.checked);
                document.querySelectorAll('.tier-hidden').forEach(el => el.disabled = !this.checked);
            });
        })();
    </script>

    <%- include('../partials/tail') %>